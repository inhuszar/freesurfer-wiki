---
title: "dmri_paths"
type: tool
fs_version: "8.2.0"
source_language: "C++"
source_files:
  - "trc/dmri_paths.cxx"
  - "trc/coffin.h"
  - "trc/coffin.cxx"
families:
  - "dmri_*"
recon_all_stage: null
related:
  - "[[dmri_train]]"
  - "[[dmri_pathstats]]"
  - "[[dmri_group]]"
  - "[[dmri_spline]]"
  - "[[bbregister]]"
status: draft
confidence: medium
last_agent_update: 2026-04-21
gaps:
  - "Prior file formats and their generation by dmri_train not fully characterized"
  - "MCMC sampling details (acceptance ratio, proposal distribution) in coffin.cxx not read"
tags:
  - diffusion
  - tractography
  - probabilistic
  - bayesian
  - mcmc
  - tracula
---

# dmri_paths

## Summary

`dmri_paths` performs probabilistic global tractography using a Bayesian MCMC (Markov Chain Monte Carlo) framework. Given diffusion MRI data, anatomical priors (trained by `dmri_train`), and end-region-of-interest (ROI) masks, it samples from the posterior distribution over the spatial course of white-matter fiber pathways to produce a probabilistic path map for each white-matter tract of interest.

## Source Information

- **Language:** C++
- **Source files:** `trc/dmri_paths.cxx`, `trc/coffin.h`, `trc/coffin.cxx`
- **Binary:** `/usr/local/freesurfer/8.2.0/bin/dmri_paths`
- **Original author:** Anastasia Yendiki (MGH)
- **Core algorithm:** `Coffin` class in `coffin.cxx`

## Purpose and Context

`dmri_paths` is the tractography engine of the **TRACULA** (TRActs Constrained by UnderLying Anatomy) pipeline. Unlike streamline tractography that follows the local diffusion direction, TRACULA uses a global probabilistic approach where:

1. Spatial priors on tract shape and anatomy (from training data) constrain plausible path configurations.
2. Diffusion data provides a likelihood for the local fiber orientation at each path point.
3. MCMC sampling explores the posterior over path configurations.

This produces a spatial probability map (not a set of streamlines) indicating how likely each voxel is to be traversed by the tract.

## Inputs

From global variables in `main()`:

| Variable | Description |
|----------|-------------|
| `dwiFile` | Diffusion MRI volume |
| `gradFile` | Gradient direction table |
| `bvalFile` | B-value table |
| `maskFile` | Brain mask |
| `bedpostDir` | BEDpostX output directory (fiber orientation distributions) |
| `roiFile1`<br>`roiFile2` | Start/end ROI volume files |
| `roiMeshFile1`<br>`roiMeshFile2` | Start/end ROI surface mesh files (optional) |
| `roiRefFile1`<br>`roiRefFile2` | Reference volumes for ROIs |
| `xyzPriorFile0/1` | Spatial position prior files |
| `tangPriorFile` | Tangent direction prior files |
| `curvPriorFile` | Curvature prior files |
| `neighPriorFile` | Neighborhood anatomy prior files |
| `localPriorFile` | Local anatomy prior files |
| `asegList` | Anatomical segmentation volumes |
| `outDir` | Output directories (one per tract) |

**Prior files** are generated by `dmri_train` from training subjects.

## Outputs

For each tract, the output directory contains:

| Output | Description | Format |
|--------|-------------|--------|
| `path.pd.nii.gz` | Posterior probability density map | NIfTI |
| `path.map.nii.gz` | Visited voxel count map | NIfTI |
| `path.samples.nii.gz` | Sample path collection | NIfTI |
| Path statistics | Per-voxel measures (from `dmri_pathstats`) | text |

## Mathematical Foundations

TRACULA uses a **Bayesian global tractography** model. The posterior over path $\mathcal{P}$:

$$
p(\mathcal{P} | \mathbf{Y}, \boldsymbol{\theta}_0) \propto p(\mathbf{Y} | \mathcal{P}) \cdot p(\mathcal{P} | \boldsymbol{\theta}_0)
$$

where $\mathbf{Y}$ is the diffusion MRI data and $\boldsymbol{\theta}_0$ are trained prior parameters.

**Likelihood** $p(\mathbf{Y} | \mathcal{P})$: Measured using fiber orientation distributions from BEDpostX. At each path point, the likelihood is high if the path direction aligns with the inferred fiber orientation.

**Prior** $p(\mathcal{P} | \boldsymbol{\theta}_0)$ encodes:
- **Spatial (xyz) prior:** Probability of path passing through each voxel, learned from training subjects
- **Tangent (direction) prior:** Probability of fiber orientation at each point
- **Curvature prior:** Penalizes sharp bends
- **Neighborhood anatomy prior:** Conditioned on local anatomical parcellation context
- **Local anatomy prior:** Voxel-level anatomical probability

The path is represented as a piecewise-linear curve parameterized by control points. MCMC (Metropolis-Hastings) sampling explores the space of control point configurations.

> [!internal] Core algorithm
> The MCMC sampling, proposal distributions, and acceptance ratios are implemented in `trc/coffin.cxx` (the `Coffin` class, named as a whimsical reference to the Bayesian framework). This class was not read in full.

## Configuration Options

### Basic inputs (native DWI space)

| Flag | Argument | Default | Description |
|------|----------|---------|-------------|
| `--outdir` | `<dir> [...]` | — | Output directory; one per path; required |
| `--indir` | `<dir> [...]` | — | Input subject directory (optional); if specified, all basic input filenames are relative to this; specify multiple for longitudinal data |
| `--dwi` | `<file>` | — | DWI volume series; required |
| `--grad` | `<file>` | — | Text file of diffusion gradients; required |
| `--bval` | `<file>` | — | Text file of diffusion b-values; required |
| `--mask` | `<file>` | — | Brain mask volume; required |
| `--bpdir` | `<dir>` | — | BEDpostX output directory (fiber orientation distributions); required |
| `--ntr` | `<num>` | `1` | Maximum number of tracts per voxel |
| `--fmin` | `<num>` | `0` | Tract volume fraction threshold |
| `--basereg` | `<file> [...]` | — | Base-to-DWI registration `.mat` file(s); required for longitudinal data; one per input directory |

### Longitudinal inputs (base template space)

| Flag | Argument | Default | Description |
|------|----------|---------|-------------|
| `--basemask` | `<file>` | — | Base template mask volume |

### End ROIs (atlas space)

| Flag | Argument | Default | Description |
|------|----------|---------|-------------|
| `--roi1` | `<file> [...]` | — | End ROI 1; volume or label file; one per path; required |
| `--roi2` | `<file> [...]` | — | End ROI 2; volume or label file; one per path; required |
| `--roimesh1` | `<file> [...]` | — | Surface mesh for end ROI 1; required when ROI 1 is a `.label` file |
| `--roimesh2` | `<file> [...]` | — | Surface mesh for end ROI 2; required when ROI 2 is a `.label` file |
| `--roiref1` | `<file> [...]` | — | Reference volume for end ROI 1; required when ROI 1 is a `.label` file |
| `--roiref2` | `<file> [...]` | — | Reference volume for end ROI 2; required when ROI 2 is a `.label` file |

### Prior-related inputs (atlas space)

| Flag | Argument | Default | Description |
|------|----------|---------|-------------|
| `--prior` | `<file0> <file1> [...]` | — | Spatial (xyz) path priors as negative log-likelihoods; paired files: off-path and on-path; one pair per path |
| `--tprior` | `<file> [...]` | — | Path tangent vector priors (negative log-likelihood); one per path |
| `--cprior` | `<file> [...]` | — | Path curvature priors (negative log-likelihood); one per path |
| `--nprior` | `<priorfile> <idfile> [...]` | — | Near-neighbor label priors; paired files: negative log-likelihood and label ID list; one pair per path |
| `--nset` | `<num>` | `14` | Subset of near-neighbor label priors to use (valid values: `6` or `14`) |
| `--lprior` | `<priorfile> <idfile> [...]` | — | Local-neighbor label priors; paired files: negative log-likelihood and label ID list; one pair per path |
| `--lset` | `<num>` | `15` | Subset of local-neighbor label priors to use (valid values: `1`, `7`, or `15`) |
| `--seg` | `<file> [...]` | — | Anatomical segmentation map of test subject; required when `--nprior` or `--lprior` is used; specify multiple for longitudinal data |
| `--reg` | `<file>` | — | DWI-to-atlas affine registration (`.mat`) |
| `--regnl` | `<file>` | — | DWI-to-atlas nonlinear registration (`.m3z`) |

### MCMC options

| Flag | Argument | Default | Description |
|------|----------|---------|-------------|
| `--init` | `<file> [...]` | — | Text file of initial control points; one per path; required |
| `--nb` | `<num>` | `5000` | Number of MCMC burn-in samples |
| `--ns` | `<num>` | `5000` | Number of MCMC post-burn-in samples |
| `--nk` | `<num>` | `10` | Keep every N-th MCMC sample |
| `--nu` | `<num>` | `40` | Update proposal distribution every N-th sample |
| `--sdp` | `<file> [...]` | — | Text file with initial proposal standard deviations for control point perturbations; one per path; if absent, SD=1 is used for all control points and paths |

### Other options

| Flag | Argument | Default | Description |
|------|----------|---------|-------------|
| `--debug` | — | off | Enable debugging output |
| `--checkopts` | — | off | Check options and exit without running |
| `--help` | — | — | Print usage information |
| `--version` | — | — | Print version and exit |

## Typical Use Cases

```bash
# In the TRACULA pipeline, dmri_paths is called via trac-all:
trac-all -c /data/subject01/dmri/dmrirc -prep -bedp -path
```

## Pipeline Context

`dmri_paths` is the core computation step of the TRACULA pipeline:

```
recon-all (anatomy) --|
dmri_bset (preprocessing) --> BEDpostX --> dmri_paths --> dmri_pathstats --> dmri_group
dmri_train (priors) --------|
```

## Gotchas and Caveats

> [!gotcha] BEDpostX prerequisite
> `dmri_paths` requires BEDpostX output (fiber orientation distributions from FSL). This must be run separately before calling `dmri_paths`.

> [!gotcha] MCMC requires many iterations
> The default 5000 burn-in + 5000 sampling iterations make this computationally expensive. Runtime can be hours per tract per subject.

> [!gotcha] Prior files must match atlas space
> The prior files (`xyzPriorFile`, `tangPriorFile`, etc.) generated by `dmri_train` encode spatial information in a specific atlas space. The test subject must be registered to this space.

## Related Tools

- [[dmri_train]] — trains the priors used by this tool
- [[dmri_pathstats]] — computes statistics from the path posteriors
- [[dmri_group]] — aggregates path statistics across subjects
- [[dmri_spline]] — spline interpolation utility for path control points
- [[bbregister]] — registers DWI to the FreeSurfer anatomy

## Confidence and Gaps

> [!gap] Coffin class implementation
> The MCMC algorithm details are in `trc/coffin.cxx` which was not read.

